%% init_parameters.m  系统参数初始化脚本
%  加载模型参数（create_model_1），配置计算中心划分与仿真参数。
%  模型定义与参数配置分离，更换模型时只需替换 create_model_1 调用。

clear; clc;

% ---- 添加路径 ----
addpath(genpath('../../../../../utils/'));
addpath(genpath('../lib/'));
addpath(genpath('../scripts/'));

% ---- 加载交互导向模型（Model 1） ----
create_model_1;

% ====================================================
%  计算中心区域划分
%     n_omega          — 计算中心数量
%     indices_omega  — 各计算中心管辖的子系统索引
%     水箱3、4无传感器，需与有传感器的水箱归入同一中心
% ====================================================
n_omega = 2;
indices_omega = {[1, 3], [2, 4]};

% ====================================================
%  仿真参数
% ====================================================
T_sim   = 1000;        % 仿真步长
k_fault = 500;         % 故障注入时刻（步数）
